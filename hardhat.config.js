require("@nomicfoundation/hardhat-toolbox");
require("dotenv").config(); // allows us to read our environment variables inside our HardHat project

/** @type import('hardhat/config').HardhatUserConfig */
module.exports = {
  solidity: "0.8.9",
};
